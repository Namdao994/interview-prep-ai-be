export const SESSION_SEARCH_FIELDS = ['targetRole', 'topicsToFocus']

export enum SESSION_STATUS {
  ACTIVE = 'ACTIVE',
  ARCHIVED = 'ARCHIVED'
}

export enum SESSION_SETUP_STATUS {
  CREATED = 'CREATED',
  INITIALIZING = 'INITIALIZING',
  READY = 'READY'
}
